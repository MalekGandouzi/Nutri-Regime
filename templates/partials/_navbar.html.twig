<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
	<div class="container">
		<a class="navbar-brand fw-bold" href="{{ path('admin_regime_list') }}">
			<span style="color:#2e7d32;">Nutri</span>
			<span style="color:#1b5e20;">Regime</span>
		</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mainNavbar">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">

				<li class="nav-item me-2">
					<a class="nav-link {% if app.request.get('_route') starts with 'admin_regime_' %}active{% endif %}" href="{{ path('admin_regime_list') }}">
						<i class="bi bi-phone"></i>
						Régimes
					</a>
				</li>

				<li class="nav-item me-2">
					<a class="nav-link {% if app.request.get('_route') starts with 'admin_plat_' %}active{% endif %}" href="{{ path('admin_plat_list') }}">
						<i class="bi bi-egg-fried"></i>
						Plats
					</a>
				</li>

				<li class="nav-item ms-lg-3 mt-2 mt-lg-0">
					<a class="btn btn-outline-danger btn-sm" href="{{ path('app_logout') }}">
						<i class="bi bi-box-arrow-right me-1"></i>
						Déconnexion
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
